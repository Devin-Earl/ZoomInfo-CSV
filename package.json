{
  "name": "zoominfo-csv",
  "version": "1.0.0",
  "description": "a Node script to call the ZoomInfo API either by JWT or by using creds to call ZI API and write the response to CSV",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Devin Earl",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "fetch": "^1.1.0",
    "jwt-simple": "^0.5.6",
    "node-fetch": "^2.6.1",
    "zoominfo-api-auth-client": "^1.0.1"
  }
}
